---
kind: pipeline
name: ubuntuarm

platform:
  os: linux
  arch: arm64
instance:
  include:
    - drone.fdiopoc.net
    - 5346a318.ngrok.io
    - foobar.ngrok.io

steps:
- name: vppbuild
  image: index.docker.io/snergster/ubuntu18arm
  commands:
  - ls -l
  - git pull || true
  - make UNATTENDED=yes verify
  when:
    event:
    - push
